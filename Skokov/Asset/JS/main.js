new WOW().init();
 
 var typed = new Typed('#typed', {
    stringsElement: '#typed-strings',
     typeSpeed:200,
     backSpeed:40,
     loop: true,
     loopCount:10,
      showCursor:true,
     cursorChar:"|",
  });
$(document).ready(function(){
    $(".splitchar").splitchar();
});